# Benchmarks

FastMSS benchmarks are organized by scope:

- `micro/` - low-level CPU/GPU microbenchmarks
- `operators/` - Conv, GEMM, Attention, FFT and other operators
- `models/` - complete model/network benchmarks
- `end-to-end/` - audio input to separated output
- `results/` - local benchmark outputs; not committed
