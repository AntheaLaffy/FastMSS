# Runtime References

Candidate, experimental, and fallback runtimes investigated by FastMSS.

The primary GPU research path is CubeCL. Other runtimes are retained for
comparison and fallback research.

See `../manifest.toml` for upstream repositories and pinned revisions.
