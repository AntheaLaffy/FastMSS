# Inference References

Reference implementations used to verify model architecture, preprocessing,
checkpoint compatibility, and inference correctness.

See `../manifest.toml` for upstream repositories and pinned revisions.
